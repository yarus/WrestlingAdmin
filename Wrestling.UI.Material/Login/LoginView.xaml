<UserControl x:Class="Wrestling.UI.Material.Login.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <materialDesign:Card Width="220" Margin="8 2 4 16" Height="170" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel>
                <TextBlock Text="Имя пользователя:" Margin="10,10,10,0"></TextBlock>
                <TextBox Margin="10,0,10,0" TabIndex="0" Loaded="UserNameBox_Loaded"
                         Text="{Binding Path=UserName, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Text="Пароль:" Margin="10,10,10,0"></TextBlock>
                <PasswordBox x:Name="PasswordBox" Margin="10,0,10,0" TabIndex="1">
                    <PasswordBox.InputBindings>
                        <KeyBinding Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=PasswordBox}" Key="Enter"/>
                    </PasswordBox.InputBindings>
                </PasswordBox>
                <TextBlock Text="{Binding Validation}" Margin="10,5,10,5" Foreground="Red"></TextBlock>
                <Button Margin="10,0,10,0" Content="Войти" Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=PasswordBox}"></Button>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>
