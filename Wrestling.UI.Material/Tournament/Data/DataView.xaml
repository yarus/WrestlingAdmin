<UserControl x:Class="Wrestling.UI.Material.Tournament.Data.DataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:md="https://github.com/fantasticfiasco/mvvm-dialogs"
             md:DialogServiceViews.IsRegistered="True"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="32">
        <ItemsControl ItemsSource="{Binding Cards}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <materialDesign:Card Width="280" Margin="0,0,16,16" UniformCornerRadius="8" materialDesign:ElevationAssist.Elevation="Dp2">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <materialDesign:PackIcon Grid.Row="0"
                                                     Kind="{Binding IconKind}"
                                                     Width="48" Height="48"
                                                     HorizontalAlignment="Left"
                                                     Margin="16,16,16,8" />
                            <StackPanel Grid.Row="1" Margin="16,0,16,8">
                                <TextBlock FontWeight="Bold" FontSize="16" Text="{Binding Title}" TextWrapping="Wrap" />
                                <TextBlock Margin="0,4,0,0" Opacity="0.7" Text="{Binding Description}" TextWrapping="Wrap" />
                            </StackPanel>
                            <Grid Grid.Row="2" Margin="16,0,16,16">
                                <Button HorizontalAlignment="Left"
                                        Style="{StaticResource MaterialDesignRaisedButton}"
                                        Command="{Binding ExecuteCommand}"
                                        IsEnabled="{Binding IsBusy, Converter={StaticResource BoolInverter}}">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="Download" Margin="0,0,6,0" />
                                        <TextBlock Text="Скачать" />
                                    </StackPanel>
                                </Button>
                                <ProgressBar HorizontalAlignment="Right"
                                             VerticalAlignment="Center"
                                             Width="24" Height="24"
                                             Style="{StaticResource MaterialDesignCircularProgressBar}"
                                             IsIndeterminate="True"
                                             Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}" />
                            </Grid>
                        </Grid>
                    </materialDesign:Card>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>
</UserControl>
