<UserControl x:Class="Wrestling.UI.Material.Slider.Slides.CarpetBracketsSlide.CarpetBracketsSlideSettings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" d:DesignHeight="120" d:DesignWidth="300">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ComboBox Grid.Row="0" Grid.Column="0" materialDesign:HintAssist.Hint="Ковер"
                  Style="{DynamicResource MaterialDesignFloatingHintComboBox}"
                  IsEditable="False" Margin="0 0 0 5" FontSize="18"
                  ItemsSource="{Binding Carpets}" SelectedItem="{Binding SelectedCarpet}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Margin="0" Padding="0" Text="{Binding Name}" />
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <TextBlock Grid.Row="1" Margin="4 6 4 6" FontSize="13"
                   Foreground="{DynamicResource MaterialDesignBodyLight}"
                   TextWrapping="Wrap">
            <Run Text="Будет добавлено слайдов:" />
            <Run Text="{Binding PlannedSlidesCount, Mode=OneWay}" />
        </TextBlock>

        <TextBlock Grid.Row="2" Margin="4 0 4 4" FontSize="11"
                   Foreground="{DynamicResource MaterialDesignBodyLight}"
                   TextWrapping="Wrap">
            Для каждой группы выбранного ковра создаётся отдельный слайд «Сетка группы». Группы, которые уже есть в канале, пропускаются.
        </TextBlock>

    </Grid>
</UserControl>
