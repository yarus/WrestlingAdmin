<model:ChildWindowBase x:Class="Wrestling.UI.Material.Tournament.Print.PrintView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:model="clr-namespace:Wrestling.UI.Material.Model"
             xmlns:codeDisplayer="clr-namespace:CodeDisplayer;assembly=CodeDisplayer"
             xmlns:schedule="clr-namespace:Wrestling.UI.Material.Tournament.Print.PrintSchedule"
             xmlns:printBracket="clr-namespace:Wrestling.UI.Material.Tournament.Print.PrintBracket"
             xmlns:printResults="clr-namespace:Wrestling.UI.Material.Tournament.Print.PrintResults"
             xmlns:printApplications="clr-namespace:Wrestling.UI.Material.Tournament.Print.PrintApplications"
             xmlns:printTeamApplication="clr-namespace:Wrestling.UI.Material.Tournament.Print.PrintTeamApplication"
             mc:Ignorable="d" Foreground="Black" Background="LightGray"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="13"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             FontFamily="{DynamicResource MaterialDesignFont}"
             Icon="../../Resources/scoreboard.ico"
             Title="Печать"
             d:DesignHeight="600" d:DesignWidth="960">

    <model:ChildWindowBase.Resources>
        <ResourceDictionary>
            <DataTemplate DataType="{x:Type schedule:PrintScheduleViewModel}">
                <schedule:PrintScheduleView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type printBracket:PrintBracketViewModel}">
                <printBracket:PrintBracketView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type printResults:PrintOlympicTeamResultsViewModel}">
                <printResults:PrintOlympicTeamResultsView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type printResults:PrintMedalsTeamResultsViewModel}">
                <printResults:PrintMedalsTeamResultsView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type printResults:PrintPointsTeamResultsViewModel}">
                <printResults:PrintPointsTeamResultsView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type printResults:PrintPersonalResultsViewModel}">
                <printResults:PrintPersonalResultsView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type printResults:PrintAchievementNominantsViewModel}">
                <printResults:PrintAchievementNominantsView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type printApplications:PrintApplicationsViewModel}">
                <printApplications:PrintApplicationsView />
            </DataTemplate>
            
            <DataTemplate DataType="{x:Type printTeamApplication:PrintTeamApplicationViewModel}">
                <printTeamApplication:PrintTeamApplicationView />
            </DataTemplate>

        </ResourceDictionary>
    </model:ChildWindowBase.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <materialDesign:ColorZone Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Padding="16" materialDesign:ShadowAssist.ShadowDepth="Depth2" Mode="PrimaryMid">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="22" Text="Печать" />

                <StackPanel Grid.Row="0" Grid.Column="1">
                    <Button Margin="0,0,20,0" Padding="0" Background="Transparent" BorderThickness="0" Foreground="White" Click="ButtonBase_OnClick">
                        <materialDesign:PackIcon Kind="Printer" VerticalAlignment="Center" Width="32" Height="32"/>
                    </Button>
                </StackPanel>
            </Grid>
        </materialDesign:ColorZone>

        <ScrollViewer Grid.Row="1" Grid.Column="1" Width="22cm" Margin="0" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="PrintControl" Background="White">
                <codeDisplayer:XamlDisplayerHost Content="{Binding }" x:Name="ContentControl" Margin="10" />
            </StackPanel>
        </ScrollViewer>
        
    </Grid>

</model:ChildWindowBase>